{
  "buildCommand": "export NODE_OPTIONS='--max-old-space-size=4096' && prisma generate && next build --turbopack",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "build": {
    "env": {
      "DATABASE_URL": "@database_url",
      "DATABASE_URL_UNPOOLED": "@database_url_unpooled"
    }
  }
}
